# Implementation Plans

Generated by the improve skill on 2026-08-16. Execute in the order below unless dependencies say otherwise. Each executor must read its plan fully, honor its STOP conditions, and update its row when done.

The plans target commit `21884fc`. Recon verified `pnpm run type-check` and the focused 47-test PHP baseline (409 assertions) at that commit. The existing untracked files `app/Console/Commands/ClearData.php` and `tests/Feature/ClearDataCommandTest.php` belong to the operator and must not be changed.

At generation time this entire `plans/` directory is also untracked. Prefer committing the plan suite before implementation. If it remains untracked, treat all six plan files as baseline artifacts: numbered plans are read-only and only `plans/README.md` may receive status edits. Always inspect `git status --short --untracked-files=all` and stage explicit in-scope paths; never use `git add -A` while the operator files remain untracked.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make `/add` the sole add launcher | P1 | M | — | DONE |
| 002 | Collapse Today meals into one list and one sheet | P1 | L | — | DONE |
| 003 | Make Progress the home for body tracking | P1 | L | — | DONE |
| 004 | Separate registration from onboarding | P1 | L | — | DONE |
| 005 | Replace macro wheels with presets and a balanced custom split | P2 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- The plans are behaviorally independent. Plans 003 and 004 both edit `routes/web.php`, so run them sequentially and resolve against the live route file rather than applying stale hunks.
- Plan 001 establishes `/add` as the canonical launcher; Plan 002 already links to that existing route and does not require Plan 001 to land first.
- Plans 003 and 004 both reuse `resources/js/bodyUnits.ts`; neither should duplicate its conversions.
- No plan adds a runtime dependency. Plan 005 requires explicit approval of its three exact ratios before code, and uses Node's built-in test runner; it must also stop if CI is not on Node 22.18 or newer.
- Native device build/run commands are intentionally absent. If device validation is requested, first ask the operator whether the target is iOS or Android; never run NativePHP build/run commands as an agent.

## Findings considered and rejected

- Merge Today and Weekly: rejected because logging a day and reviewing a week are distinct jobs; combining them would make the already-large Today page harder to scan.
- Merge Weekly and macro detail: rejected because Weekly compares aggregate days while macro detail explains one day's meal attribution.
- Merge nutrition Goals into Progress: rejected because target setup is occasional configuration, while Progress is a repeated body-measurement workflow.
- Merge Photo meal into manual Food: rejected because photo analysis has asynchronous upload, review, follow-up, and error states that would complicate the quick food path.
- Merge login, forgot-password, reset-password, and verification URLs: rejected because they already share `Account.vue` while remaining correctly deep-linkable lifecycle states.
- Split the slimmer Settings screen further: rejected because removing body profile already leaves one coherent settings destination; more routes would add navigation without removing meaningful complexity.
- Treat all audited screenshots as pages: rejected because the 66 captures represented interaction states across nine Vue pages, not 66 separate screens.
